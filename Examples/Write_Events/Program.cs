namespace Write_Events
{
    class Program
    {
        static void Main(string[] args)
        {
            // need an object for event handlers.  Show example in Session class
            Session session = new Session();
            session.Main();
        }
    }
}
